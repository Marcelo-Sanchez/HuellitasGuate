@page
@model RegisterModel
@{
    ViewData["Title"] = "Registro";
}

<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        /* Personaliza el estilo aquí si es necesario */
        body {
            background-color: #f0f0f0;
        }

        .container {
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            padding: 30px;
            margin-top: 50px;
        }

        .form-title {
            font-size: 24px;
            margin-bottom: 20px;
            text-align: center;
        }

        .form-group label {
            font-weight: bold;
        }

        .btn-register {
            background-color: #007bff;
            border: none;
            border-radius: 4px;
            color: #fff;
            padding: 10px 20px;
            font-size: 18px;
            margin-top: 20px;
        }

            .btn-register:hover {
                background-color: #0056b3;
            }
    </style>
    <title>@ViewData["Title"]</title>
</head>
<body>

    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <form id="registerForm" asp-route-returnUrl="@Model.ReturnUrl" method="post">
                    <h2 class="form-title">-Registrate para poder ingresar-</h2>
                    <hr />
                    <div asp-validation-summary="ModelOnly" class="alert alert-danger mt-3"></div>
                    <div class="mb-3 form-group">
                        <label for="Input_Email">Correo Electrónico</label>
                        <input asp-for="Input.Email" class="form-control" id="Input_Email" autocomplete="username" aria-required="true" />
                        <span asp-validation-for="Input.Email" class="text-danger"></span>
                    </div>
                    <div class="mb-3 form-group">
                        <label for="Input_Password">Contraseña</label>
                        <input asp-for="Input.Password" class="form-control" id="Input_Password" autocomplete="new-password" aria-required="true" />
                        <span asp-validation-for="Input.Password" class="text-danger"></span>
                    </div>
                    <div class="mb-3 form-group">
                        <label for="Input_ConfirmPassword">Confirmar Contraseña</label>
                        <input asp-for="Input.ConfirmPassword" class="form-control" id="Input_ConfirmPassword" autocomplete="new-password" aria-required="true" />
                        <span asp-validation-for="Input.ConfirmPassword" class="text-danger"></span>
                    </div>
                    <button id="registerSubmit" type="submit" class="btn btn-register w-100">Registrar</button>
                </form>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
